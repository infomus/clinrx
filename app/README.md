# ClinRx App

Planning and implementation workspace for the ClinRx application.

Start with [PLAN.md](PLAN.md).

The active implementation now lives in `apps/mobile` and shared `packages/*` per `ARCHITECTURE.md`.
